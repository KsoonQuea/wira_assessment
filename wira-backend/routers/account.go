package router

import (
	"github.com/gin-gonic/gin"
)

type AccountRouter struct{}

func (s *AccountRouter) InitAccountRouter(Router *gin.RouterGroup) {

	// accountRouter := Router.Group("base") 
	
}
